@model IEnumerable<DnDFrontEnd.Models.Persuasion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Persausionprof)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Persausionprof)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Persuasionid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Persuasionid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Persuasionid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
