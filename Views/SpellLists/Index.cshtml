@model IEnumerable<DnDFrontEnd.Models.SpellList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Character)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Character.PlayerCharacterid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Spelllistid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Spelllistid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Spelllistid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
