@model IEnumerable<DnDFrontEnd.Models.Features>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aquiredfrom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Caharacter)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aquiredfrom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Caharacter.PlayerCharacterid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Featuresid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Featuresid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Featuresid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
