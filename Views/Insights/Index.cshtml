@model IEnumerable<DnDFrontEnd.Models.Insight>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Insighprof)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Insighprof)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Insightid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Insightid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Insightid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
