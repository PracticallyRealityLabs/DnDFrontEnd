@model DnDFrontEnd.Models.SpellListHasSpells

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>SpellListHasSpells</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SpellListHasSpellsid" />
            <div class="form-group">
                <label asp-for="SpellListId" class="control-label"></label>
                <select asp-for="SpellListId" class="form-control" asp-items="ViewBag.SpellListId"></select>
                <span asp-validation-for="SpellListId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Spellid" class="control-label"></label>
                <select asp-for="Spellid" class="form-control" asp-items="ViewBag.Spellid"></select>
                <span asp-validation-for="Spellid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
