@model IEnumerable<DnDFrontEnd.Models.AnimalHandling>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Animalhandprof)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Animalhandprof)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AnimalHandlingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AnimalHandlingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AnimalHandlingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
